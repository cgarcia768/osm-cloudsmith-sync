name: Sync OpenSourceMalware → Cloudsmith EPM (all severities)

on:
  schedule:
    # Run every hour at :15 past
    - cron: '15 * * * *'
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Restore state cache
        uses: actions/cache@v4
        with:
          path: |
            last_known_packages.json
            osv_overlap_report.json
          key: osm-state-${{ github.run_id }}
          restore-keys: |
            osm-state-

      - name: Run sync script
        env:
          CLOUDSMITH_API_KEY: ${{ secrets.CLOUDSMITH_API_KEY }}
          CLOUDSMITH_WORKSPACE: ${{ secrets.CLOUDSMITH_WORKSPACE }}
          CLOUDSMITH_POLICY_SLUG: ${{ secrets.CLOUDSMITH_POLICY_SLUG }}
        run: python sync_osm_to_cloudsmith.pyame: Sync OpenSourceMalware → Cloudsmith EPM (all severities)

on:
  schedule:
    # Run every hour at :15 past
    - cron: '15 * * * *'
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Restore state cache
        uses: actions/cache@v4
        with:
          path: last_known_packages.json
          key: osm-state-${{ github.run_id }}
          restore-keys: |
            osm-state-

      - name: Run sync script
        env:
          CLOUDSMITH_API_KEY: ${{ secrets.CLOUDSMITH_API_KEY }}
          CLOUDSMITH_WORKSPACE: ${{ secrets.CLOUDSMITH_WORKSPACE }}
          CLOUDSMITH_POLICY_SLUG: ${{ secrets.CLOUDSMITH_POLICY_SLUG }}
        run: python sync_osm_to_cloudsmith.py
